




/*
// GET

function getCards(longitude, latitude) {
  performRequest('https://www.apimeteo.fr?lon=' + longitude + '&lat='+ latitude, 'GET', {
    session_id: sessionId,
    "_items_per_page": 100
  }, function(data) {
    console.log('Fetched ' + data.result.paging.total_items + ' cards');
  });
}
*/
